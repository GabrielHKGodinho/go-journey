{
  "doneMap": {
    "2026-07-28": true,
    "2026-07-29": true,
    "2026-07-30": true,
    "2026-07-31": true,
    "2026-08-01": true,
    "2026-08-02": true,
    "2026-08-03": true,
    "2026-08-04": true,
    "2026-08-05": true
  },
  "notesMap": {
    "2026-07-28": "\"A package is Go's general unit of code organization — every file belongs to one. Most packages become reusable libraries that other files import. main is the exception: instead of being importable, it compiles into the actual executable — it's the entry point of the program.\"\n\npackage main\nimport (\"fmt\", \"math/rand\")\n\nmath.Pi\n\nfunc swap(x, y int) (w, z string) {\n      w = \"hello\"\n      return\n}\n\nvar i, j = 1, 2\nfunc(){\n   i := 3\n}",
    "2026-07-29": "defer stacks a call to run only when the surrounding function returns, LIFO order. The idiomatic pattern is: open a resource, check the error, and immediately follow with the defer that closes it — that guarantees cleanup no matter how many return paths the function has\n\nnao existe while\n\nif valor, ok := algumMapa[\"chave\"]; ok {}\n\ndefer file.Close()\n",
    "2026-07-30": "A slice holds a pointer into a backing array, plus len and cap. When you slice a large array/slice, that pointer still points inside the big array — so even though the small slice only 'sees' a few elements, the whole backing array stays pinned in memory as long as that slice is alive, since the garbage collector can't free something still referenced. That's the leak: actual memory usage is much bigger than len suggests.\n\n\ns := make([]int, 0)\n\tfor i := 0; i < 10; i++ {\n\t\ts = append(s, i)\n\t\tfmt.Printf(\"len=%d cap=%d\\n\", len(s), cap(s))\n\t}",
    "2026-07-31": "When a closure references a variable from its enclosing scope, Go's escape analysis detects that and allocates the variable on the heap instead of the stack. It's only garbage-collected once there's no live reference to it left — typically when the closure using it also goes out of scope\n\nmaps\nidade := make(map[string]int)\nvalor, ok := idade[\"Pedro\"]\n\nfunction values\nfunc quadrado(x int) int{...}\nvar operacao func(int) int = quadrado\nfmt.Println(quadrado(5))\n\nfunction closure\nfunc contador() func() int {\n   cont := 0\n   return func() int {\n      cont++\n      return cont\n   }\n}\nproximo := contador()\nfmt.Println(proximo()) //1",
    "2026-08-02": "1. Nao tem while\n2. declarar variavel junto com if\n3. defer",
    "2026-08-03": "Go doesn't have classes or inheritance — it uses composition and interfaces to achieve the same encapsulation and polymorphism goals. A struct only declares data; methods are attached externally via a receiver, not inside the type definition. And unlike a PHP object (passed by reference), a struct is passed by value by default — assigning it or passing it as an argument creates a copy, unless you use a pointer\n\nComo funciona controle de acesso/visibilidade em Go?\nIn Go, visibility doesn't use keywords like public/private — it's determined by the first letter of the identifier. Uppercase means exported, visible outside the package; lowercase means unexported, only accessible within the same package. This applies equally to structs, fields, functions, and variables\n\nPor que módulos em Go usam nomes tipo github.com/usuario/repo?\nThe module name acts as a globally unique namespace, usually based on the Git repository path. This avoids naming collisions between different people's modules, and it means the module is already ready to be imported by others via go get, even if it's only used locally today.",
    "2026-08-04": "When the dev intended to pass the struct by reference and endend up passing by value.\n\nComo Go implementa encapsulamento sem private/public?\nEncapsulation in Go happens at the package level, not the struct level. Exported fields and methods (capitalized) are visible outside the package; unexported ones (lowercase) are only accessible within the same package — including by other structs in that package. This means that to enforce a business rule around access to some data, you keep the field unexported and expose only controlled methods, typically from a different package than the consuming code.",
    "2026-08-05": "Implicitly and structurally: the compiler checks whether the type's method set contains every method the interface requires, with identical name, parameters, and return values. There's no explicit 'implements' declaration — satisfaction is automatic and checked at compile time. And since a pointer's method set includes both value and pointer receiver methods, while a value's method set only includes value receiver methods, that can determine whether it's T or *T that actually satisfies the interface.\n\ntype shape interface {\n\tarea() float64\n}\n\ntype circle struct {\n\tradius float64\n}\n\nfunc (c circle) area() float64 {\n\t// ...\n}\n\nfunc printShapeInfo(s shape) {\n\tc, ok := s.(circle)\n\tif ok {\n\t\tradius := c.radius\n\t\tfmt.Printf(\"s is a circle, radius: %v\\n\", radius)\n\t\treturn\n\t}\n}\n\nfunc printNumericValue(num interface{}) {\n\tswitch v := num.(type) {\n\tcase int:\n\t\tfmt.Printf(\"%T\\n\", v)\n\tcase string:\n\t\tfmt.Printf(\"%T\\n\", v)\n\tdefault:\n\t\tfmt.Printf(\"%T\\n\", v)\n\t}\n}\n\nPor que um método com receiver ponteiro não pertence ao method set do valor?\nBecause a pointer can always be safely dereferenced to access the value, but the reverse isn't guaranteed — not every value is addressable (like values inside a map). To keep a simple, exception-free rule, Go defines that a pointer's method set includes both value and pointer receiver methods, while a value's method set only includes value receiver methods. This directly affects interface satisfaction: a type with pointer receiver methods only satisfies an interface through the pointer"
  },
  "exportedAt": "2026-08-05T12:18:15.972Z"
}